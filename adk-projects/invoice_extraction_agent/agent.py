# This file is not used by the orchestrate CLI.
# The agent is defined in agent.yaml and deployed via:
#
#   orchestrate tools import -f tools.py        (register tools first)
#   orchestrate agents import -f agent.yaml     (then register the agent)
#
# The system prompt lives in agent.yaml under the `instructions` key.
